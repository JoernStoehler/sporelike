name = "sporelike-api"
main = "src/index.ts"
compatibility_date = "2024-12-01"

# GEMINI_API_KEY must be set as a Worker secret, not here.
# Run: wrangler secret put GEMINI_API_KEY
# It will never appear in this file or in the frontend bundle.
